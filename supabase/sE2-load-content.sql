-- ============================================================================
-- SPRINT E2 · LOAD CONTENT — Pasos detallados + errores comunes por tarea
-- Fuente: 8 .docx parseados automáticamente. Idempotente (UPDATE WHERE code=).
-- ============================================================================


-- 0 tareas actualizadas con steps + errores comunes.